# Representative RAVEN outputs

This directory contains a compact, text-only subset of a completed representative RAVEN run.

These files are supplied to show expected report structure, stage naming, winner reporting, timing output, and manifest content without committing large MAT files or trained model binaries.

The representative run is not a biological validation dataset and is not intended to reproduce the exact numerical results of Datasets A–C.
